matrix:
  include:
    - os: linux
      if: tag IS NOT present
    - os: osx
      osx_image: xcode10.2
      if: tag IS present

language: node_js
node_js: "12"

script:
  - npm test
  - if [ -n "$TRAVIS_TAG" ]; then npm run dist; fi
